"use client";
import { Button, Card, Eyebrow } from "../ui/primitives";
import type { View } from "../ui/shell";
export function Landing({ go }: { go: (v: View) => void }) { return <div className="landing"><div className="landing-nav"><span className="brand">oratry<span>.</span></span><Button variant="quiet" onClick={() => go("signup")}>Sign in</Button></div><div className="hero"><Eyebrow>Deliberate speaking practice</Eyebrow><h1>Speak with<br /><i>intent.</i></h1><p>Build the habits behind clear, confident communication — one focused practice at a time.</p><Button onClick={() => go("signup")}>Start practicing <span aria-hidden>→</span></Button></div><div className="hero-note">Structured challenges · private recordings · one clear focus</div></div>; }
export function Signup({ go }: { go: (v: View) => void }) { return <div className="auth"><div><span className="brand">oratry<span>.</span></span><Eyebrow>Your practice starts here</Eyebrow><h1>Make your words<br />count.</h1><p>Two minutes of thoughtful speaking can change the way you show up.</p></div><Card><h2>Create your account</h2><label>Email<input type="email" placeholder="you@example.com" /></label><label>Password<input type="password" placeholder="At least 8 characters" /></label><label className="check"><input type="checkbox" /> I agree to the terms and privacy policy.</label><Button onClick={() => go("onboarding")}>Create account</Button><p className="muted">Already have an account? <button onClick={() => go("home")}>Sign in</button></p></Card></div>; }
export function Onboarding({ go }: { go: (v: View) => void }) { return <div className="onboarding narrow"><Eyebrow>01 / 02</Eyebrow><h1>What would you like to improve?</h1><p>Select up to two areas. We’ll use this to tailor your first practice.</p><div className="choice-grid">{["Speak more clearly", "Organize my thoughts", "Sound more confident", "Find words faster"].map(x => <button key={x} className="choice">{x}<span>+</span></button>)}</div><Button onClick={() => go("baseline")}>Continue</Button></div>; }
export function Baseline({ go }: { go: (v: View) => void }) { return <div className="baseline narrow"><Eyebrow>Your starting point</Eyebrow><h1>Let’s find your<br /><i>baseline.</i></h1><p>You’ll complete three short speaking challenges. There are no right answers — this simply gives us a place to begin.</p><Card><span className="step-number">3</span><div><h3>Short challenges</h3><p>About 2 minutes each</p></div><span className="step-number">15</span><div><h3>Minutes total</h3><p>Take them when you’re ready</p></div></Card><Button onClick={() => go("home")}>Begin baseline</Button></div>; }
