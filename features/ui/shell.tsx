"use client";
import { Button } from "./primitives";
import type { ReactNode } from "react";
export type View = "landing" | "signup" | "activate" | "oauth" | "onboarding" | "baseline" | "home" | "practice" | "briefing" | "prepare" | "speak" | "processing" | "results" | "feedback" | "retry" | "comparison" | "progress" | "vocabulary" | "profile";
const links: { label: string; view: View }[] = [{ label: "Home", view: "home" }, { label: "Practice", view: "practice" }, { label: "Progress", view: "progress" }, { label: "Vocabulary", view: "vocabulary" }, { label: "Profile", view: "profile" }];
export function Shell({ view, setView, children, onSignOut }: { view: View; setView: (view: View) => void; children: ReactNode; onSignOut: () => void }) { return <div className="shell"><header><button className="brand" onClick={() => setView("home")} aria-label="Oratry home">oratry<span>.</span></button><nav aria-label="Primary navigation">{links.map(link => <button key={link.view} onClick={() => setView(link.view)} className={view === link.view ? "active" : ""}>{link.label}</button>)}</nav><Button variant="quiet" onClick={onSignOut} aria-label="Sign out">Sign out</Button></header><main>{children}</main><nav className="mobile-nav" aria-label="Mobile navigation">{links.map(link => <button key={link.view} onClick={() => setView(link.view)} className={view === link.view ? "active" : ""}>{link.label}</button>)}</nav></div>; }
